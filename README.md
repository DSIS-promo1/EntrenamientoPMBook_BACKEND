# Instalación

### Instrucciones
Crear la base de datos, "mydb"

Configurar .env

Ejecutar los siguientes comandos:

```
 composer install
 php artisan migrate:install
 php artisan migrate
 php artisan db:seed

```